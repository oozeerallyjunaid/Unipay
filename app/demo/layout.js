export const metadata = {
  title: "Live Demo — Send XRP & Escrow | UniPay XRPL",
  description: "Try a real XRP payment and escrow — live on the XRP Ledger Testnet.",
};

export default function DemoLayout({ children }) {
  return children;
}
